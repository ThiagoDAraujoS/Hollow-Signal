namespace Core.UI {
    /// Distinct viewpoints and panels displayed within the main menu system.
    public enum MenuViewState {
        Main = 0,
        Load = 1,
        Save = 2,
        Settings = 3,
        Credits = 4,
        Closed = 5
    }
}
